pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp maven_training/mss_info_app/index.html public/index.html
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: always
